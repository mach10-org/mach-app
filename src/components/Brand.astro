---
type Props = {
  size: 'sm' | 'md' | 'lg';
};

const { size = 'md' } = Astro.props;
---

<a href={import.meta.env.BASE_URL} class='flex items-center lg:mr-8 transition-opacity hover:opacity-80'>
  <span class='text-2xl mr-3 sm:text-3xl'>👩🏻‍🚀</span>
  <span class='self-center font-semibold whitespace-nowrap text-text-base' class:list={[{ 'text-lg': size === 'sm' }, { 'text-xl': size === 'md' }, { 'text-2xl': size === 'lg' }]}>
    {import.meta.env.PUBLIC_SITE_NAME || process.env.PUBLIC_SITE_NAME}
  </span>
</a>
