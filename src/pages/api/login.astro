---
import { supabase } from '@utils/auth';
// const { body } = await Astro.request;
// if (body && (body as any).access_token) {
//   Astro.response.headers.set('Set-Cookie', `sbat=${(body as any).access_token}; Path=/;`);
// }
---
