---
import LoginOrRegister from '@components/Auth/LoginOrRegister.vue';
import ContentLayout from '@layouts/ContentLayout.astro';

const pageData = {
  title: 'Mach App',
  description: 'Connect to Mach App'
};
---

<ContentLayout {...pageData}>
  <LoginOrRegister client:only='vue' />
</ContentLayout>
